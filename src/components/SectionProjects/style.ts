import styled from 'styled-components'
import { motion } from 'framer-motion'

export const ProjectsContain = styled(motion.section)`
  display: flex;
  flex-wrap: wrap;
  gap: 32px;
  justify-content: center;
`
